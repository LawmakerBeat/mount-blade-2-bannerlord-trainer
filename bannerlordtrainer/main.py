[
    {
        "path": "mount-blade-2-bannerlord-trainer/requirements.txt",
        "content": "pywin32>=305\npsutil>=5.9.0\n"
    },
    {
        "path": "mount-blade-2-bannerlord-trainer/src/__init__.py",
        "content": "# Mount & Blade II: Bannerlord Trainer package\n"
    },
    {
        "path": "mount-blade-2-bannerlord-trainer/src/memory_reader.py",
        "content": "\"\"\"\nMemory reader module for Bannerlord process interaction.\nHandles reading and writing process memory for game values.\n\"\"\"\n\nimport ctypes\nfrom ctypes import wintypes\nfrom typing import Optional, List, Tuple\n\n# Windows API constants and structures\nPROCESS_VM_READ = 0x0010\nPROCESS_VM_WRITE = 0x0020\nPROCESS_VM_OPERATION = 0x0008\nPROCESS_QUERY_INFORMATION = 0x0400\n\nclass MemoryReader:\n    \"\"\"Provides low-level memory operations for the Bannerlord process.\"\"\"\n\n    def __init__(self):\n        self.process_handle = None\n        self.pid = None\n        self.kernel32 = ctypes.windll.kernel32\n\n    def open_process(self, pid: int) -> bool:\n        \"\"\"Open a handle to the target process with read/write permissions.\"\"\"\n        self.pid = pid\n        self.process_handle = self.kernel32.OpenProcess(\n            PROCESS_VM_READ | PROCESS_VM_WRITE | PROCESS_VM_OPERATION | PROCESS_QUERY_INFORMATION,\n            False,\n            pid\n        )\n        return self.process_handle is not None and self.process_handle != 0\n\n    def close_process(self) -> None:\n        \"\"\"Close the handle to the target process.\"\"\"\n        if self.process_handle:\n            self.kernel32.CloseHandle(self.process_handle)\n            self.process_handle = None\n\n    def read_memory(self, address: int, size: int) -> Optional[bytes]:\n        \"\"\"Read memory from the target process at a given address.\"\"\"\n        buffer = ctypes.create_string_buffer(size)\n        bytes_read = ctypes.c_size_t(0)\n        success = self.kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            buffer,\n            size,\n            ctypes.byref(bytes_read)\n        )\n        if success and bytes_read.value == size:\n            return buffer.raw\n        return None\n\n    def write_memory(self, address: int, data: bytes) -> bool:\n        \"\"\"Write data to the target process memory.\"\"\"\n        bytes_written = ctypes.c_size_t(0)\n        success = self.kernel32.WriteProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            data,\n            len(data),\n            ctypes.byref(bytes_written)\n        )\n        return success and bytes_written.value == len(data)\n\n    def read_int32(self, address: int) -> Optional[int]:\n        \"\"\"Read a 32-bit integer from the target process memory.\"\"\"\n        data = self.read_memory(address, 4)\n        if data:\n            return int.from_bytes(data, 'little', signed=True)\n        return None\n\n    def write_int32(self, address: int, value: int) -> bool:\n        \"\"\"Write a 32-bit integer to the target process memory.\"\"\"\n        data = value.to_bytes(4, 'little', signed=True)\n        return self.write_memory(address, data)\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"Read a float (4 bytes) from the target process memory.\"\"\"\n        data = self.read_memory(address, 4)\n        if data:\n            import struct\n            return struct.unpack('<f', data)[0]\n        return None\n\n    def write_float(self, address: int, value: float) -> bool:\n        \"\"\"Write a float (4 bytes) to the target process memory.\"\"\"\n        import struct\n        data = struct.pack('<f', value)\n        return self.write_memory(address, data)\n"
    },
    {
        "path": "mount-blade-2-bannerlord-trainer/src/pattern_scanner.py",
        "content": "\"\"\"\nPattern scanner module for finding memory addresses dynamically.\nScans the Bannerlord process memory for byte patterns to locate game variables.\n\"\"\"\n\nfrom typing import Optional, List, Tuple\nfrom src.memory_reader import MemoryReader\n\nclass PatternScanner:\n    \"\"\"Scans process memory for byte patterns (AOB scanning).\"\"\"\n\n    def __init__(self, memory_reader: MemoryReader):\n        self.memory_reader = memory_reader\n        self.base_address = 0x00400000  # Default base for many x86 games\n\n    def set_base_address(self, address: int) -> None:\n        \"\"\"Set the base address for scanning (e.g., from module info).\"\"\"\n        self.base_address = address\n\n    def pattern_to_bytes(self, pattern: str) -> List[Optional[int]]:\n        \"\"\"Convert a hex pattern string like '48 8B 05 ?? ?? ?? ?? 89' to a list of ints/None.\"\"\"\n        result = []\n        for byte_str in pattern.split():\n            if byte_str == '??':\n                result.append(None)\n            else:\n                result.append(int(byte_str, 16))\n        return result\n\n    def scan_pattern(self, pattern: str, start_address: Optional[int] = None,\n                     size: int = 0x7FFFFFFF) -> Optional[int]:\n        \"\"\"\n        Scan memory for a given byte pattern.\n        Returns the address of the first match, or None.\n        \"\"\"\n        if start_address is None:\n            start_address = self.base_address\n\n        pattern_bytes = self.pattern_to_bytes(pattern)\n        pattern_len = len(pattern_bytes)\n\n        # Read memory in chunks for performance\n        chunk_size = 4096\n        current_address = start_address\n\n        while current_address < start_address + size:\n            chunk = self.memory_reader.read_memory(current_address, chunk_size)\n            if chunk is None:\n                current_address += chunk_size\n                continue\n\n            for i in range(len(chunk) - pattern_len + 1):\n                match = True\n                for j, expected in enumerate(pattern_bytes):\n                    if expected is not None:\n                        if chunk[i + j] != expected:\n                            match = False\n                            break\n                if match:\n                    return current_address + i\n\n            current_address += chunk_size - pattern_len + 1\n\n        return None\n\n    def scan_pattern_multiple(self, pattern: str, count: int,\n                              start_address: Optional[int] = None,\n                              size: int = 0x7FFFFFFF) -> List[int]:\n        \"\"\"Find multiple occurrences of a pattern.\"\"\"\n        addresses = []\n        last_address = start_address or self.base_address\n\n        for _ in range(count):\n            addr = self.scan_pattern(pattern, last_address + 1, size - (last_address - (start_address or self.base_address)))\n            if addr is None:\n                break\n            addresses.append(addr)\n            last_address = addr\n\n        return addresses\n"
    },
    {
        "path": "mount-blade-2-bannerlord-trainer/src/trainer.py",
        "content": "\"\"\"\nMain trainer module for Mount & Blade II: Bannerlord.\nProvides high-level cheat functions: gold, health, stamina, and party size.\n\"\"\"\n\nimport psutil\nfrom typing import Optional\nfrom src.memory_reader import MemoryReader\nfrom src.pattern_scanner import PatternScanner\n\nclass BannerlordTrainer:\n    \"\"\"\n    Trainer class for Mount & Blade II: Bannerlord.\n    Uses memory manipulation to modify player stats.\n    \"\"\"\n\n    # Known offsets for Bannerlord v1.2.x (example - would need updating per game version)\n    GOLD_OFFSET = 0x1A3F4C0\n    HEALTH_OFFSET = 0x1A4B200\n    STAMINA_OFFSET = 0x1A4B204\n    PARTY_SIZE_OFFSET = 0x1A5D100\n\n    def __init__(self):\n        self.memory_reader = MemoryReader()\n        self.pattern_scanner = PatternScanner(self.memory_reader)\n        self.process = None\n        self.base_address = None\n\n    def find_bannerlord_process(self) -> Optional[psutil.Process]:\n        \"\"\"Find the Bannerlord process by name.\"\"\"\n        for proc in psutil.process_iter(['pid', 'name']):\n            if proc.info['name'].lower() in ['bannerlord.exe', 'bannerlord']:\n                return proc\n        return None\n\n    def attach_to_game(self) -> bool:\n        \"\"\"Attach to the running Bannerlord process.\"\"\"\n        self.process = self.find_bannerlord_process()\n        if self.process is None:\n            return False\n\n        if not self.memory_reader.open_process(self.process.info['pid']):\n            return False\n\n        # Get base address from process memory maps\n        for region in self.process.memory_maps(grouped=False):\n            if region.path and 'Bannerlord' in region.path:\n                self.base_address = region.addr\n                self.pattern_scanner.set_base_address(self.base_address)\n                break\n\n        return self.base_address is not None\n\n    def detach_from_game(self) -> None:\n        \"\"\"Detach from the game process.\"\"\"\n        self.memory_reader.close_process()\n        self.process = None\n\n    def get_gold(self) -> Optional[int]:\n        \"\"\"Read current gold amount.\"\"\"\n        if self.base_address is None:\n            return None\n        return self.memory_reader.read_int32(self.base_address + self.GOLD_OFFSET)\n\n    def set_gold(self, amount: int) -> bool:\n        \"\"\"Set gold to a specific amount.\"\"\"\n        if self.base_address is None:\n            return False\n        return self.memory_reader.write_int32(self.base_address + self.GOLD_OFFSET, amount)\n\n    def add_gold(self, amount: int) -> bool:\n        \"\"\"Add gold to current amount.\"\"\"\n        current = self.get_gold()\n        if current is None:\n            return False\n        return self.set_gold(current + amount)\n\n    def get_health(self) -> Optional[float]:\n        \"\"\"Read current health value.\"\"\"\n        if self.base_address is None:\n            return None\n        return self.memory_reader.read_float(self.base_address + self.HEALTH_OFFSET)\n\n    def set_health(self, value: float) -> bool:\n        \"\"\"Set health to a specific value.\"\"\"\n        if self.base_address is None:\n            return False\n        return self.memory_reader.write_float(self.base_address + self.HEALTH_OFFSET, value)\n\n    def get_stamina(self) -> Optional[float]:\n        \"\"\"Read current stamina value.\"\"\"\n        if self.base_address is None:\n            return None\n        return self.memory_reader.read_float(self.base_address + self.STAMINA_OFFSET)\n\n    def set_stamina(self, value: float) -> bool:\n        \"\"\"Set stamina to a specific value.\"\"\"\n        if self.base_address is None:\n            return False\n        return self.memory_reader.write_float(self.base_address + self.STAMINA_OFFSET, value)\n\n    def get_party_size(self) -> Optional[int]:\n        \"\"\"Read current party size.\"\"\"\n        if self.base_address is None:\n            return None\n        return self.memory_reader.read_int32(self.base_address + self.PARTY_SIZE_OFFSET)\n\n    def set_party_size(self, size: int) -> bool:\n        \"\"\"Set party size limit.\"\"\"\n        if self.base_address is None:\n            return False\n        return self.memory_reader.write_int32(self.base_address + self.PARTY_SIZE_OFFSET, size)\n\n    def freeze_health(self, enable: bool, value: float = 200.0) -> None:\n        \"\"\"Continuously set health to a value (freeze).\n        This is a placeholder - actual freezing requires a loop in a separate thread.\"\"\"\n        if enable:\n            self.set_health(value)\n"
    },
    {
        "path": "mount-blade-2-bannerlord-trainer/src/cli.py",
        "content": "\"\"\"\nCommand-line interface for the Bannerlord Trainer.\nProvides interactive menu for cheats.\n\"\"\"\n\nimport sys\nimport time\nfrom src.trainer import BannerlordTrainer\n\nclass TrainerCLI:\n    \"\"\"Simple CLI interface for the trainer.\"\"\"\n\n    def __init__(self):\n        self.trainer = BannerlordTrainer()\n\n    def print_banner(self) -> None:\n        \"\"\"Print a fancy banner.\"\"\"\n        print(\"=\" * 50)\n        print(\"   Mount & Blade II: Bannerlord Trainer\")\n        print(\"=\" * 50)\n\n    def print_menu(self) -> None:\n        \"\"\"Print the main menu.\"\"\"\n        print(\"\\nOptions:\")\n        print(\"1. Attach to Bannerlord\")\n        print(\"2. Get Gold\")\n        print(\"3. Set Gold\")\n        print(\"4. Add Gold\")\n        print(\"5. Get Health\")\n        print(\"6. Set Health\")\n        print(\"7. Get Stamina\")\n        print(\"8. Set Stamina\")\n        print(\"9. Get Party Size\")\n        print(\"10. Set Party Size\")\n        print(\"0. Exit\")\n\n    def run(self) -> None:\n        \"\"\"Main loop for the CLI.\"\"\"\n        self.print_banner()\n\n        while True:\n            self.print_menu()\n            choice = input(\"\\nEnter choice: \").strip()\n\n            if choice == \"1\":\n                if self.trainer.attach_to_game():\n                    print(\"[+] Attached to Bannerlord process.\")\n                else:\n                    print(\"[-] Failed to attach. Is Bannerlord running?\")\n\n            elif choice == \"2\":\n                gold = self.trainer.get_gold()\n                if gold is not None:\n                    print(f\"[+] Gold: {gold}\")\n                else:\n                    print(\"[-] Failed to read gold.\")\n\n            elif choice == \"3\":\n                try:\n                    amount = int(input(\"Enter gold amount: \"))\n                    if self.trainer.set_gold(amount):\n                        print(f\"[+] Gold set to {amount}\")\n                    else:\n                        print(\"[-] Failed to set gold.\")\n                except ValueError:\n                    print(\"[-] Invalid number.\")\n\n            elif choice == \"4\":\n                try:\n                    amount = int(input(\"Enter gold to add: \"))\n                    if self.trainer.add_gold(amount):\n                        print(f\"[+] Added {amount} gold.\")\n                    else:\n                        print(\"[-] Failed to add gold.\")\n                except ValueError:\n                    print(\"[-] Invalid number.\")\n\n            elif choice == \"5\":\n                health = self.trainer.get_health()\n                if health is not None:\n                    print(f\"[+] Health: {health:.2f}\")\n                else:\n                    print(\"[-] Failed to read health.\")\n\n            elif choice == \"6\":\n                try:\n                    value = float(input(\"Enter health value: \"))\n                    if self.trainer.set_health(value):\n                        print(f\"[+] Health set to {value:.2f}\")\n                    else:\n                        print(\"[-] Failed to set health.\")\n                except ValueError:\n                    print(\"[-] Invalid number.\")\n\n            elif choice == \"7\":\n                stamina = self.trainer.get_stamina()\n                if stamina is not None:\n                    print(f\"[+] Stamina: {stamina:.2f}\")\n                else:\n                    print(\"[-] Failed to read stamina.\")\n\n            elif choice == \"8\":\n                try:\n                    value = float(input(\"Enter stamina value: \"))\n                    if self.trainer.set_stamina(value):\n                        print(f\"[+] Stamina set to {value:.2f}\")\n                    else:\n                        print(\"[-] Failed to set stamina.\")\n                except ValueError:\n                    print(\"[-] Invalid number.\")\n\n            elif choice == \"9\":\n                size = self.trainer.get_party_size()\n                if size is not None
